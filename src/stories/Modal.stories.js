import React from 'react';
import '../App.scss';

import Modal from '../comps/Modal';

export default {
    title: "Modal",
    component: Modal
}
export const DefaultModal = () => {
    return <Modal
    />
}